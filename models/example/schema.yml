
version: 2

models:
  - name: customers
    description: "Customers analitycs"
    columns:
      - name: id
        description: "PK"
        tests:
          - unique
          - not_null
      - name: first_name
      - name: last_name

# models:
#     - name: my_first_dbt_model
#       description: "A starter dbt model"
#       columns:
#           - name: id
#             description: "The primary key for this table"
#             tests:
#                 - unique
#                 - not_null

#     - name: my_second_dbt_model
#       description: "A starter dbt model"
#       columns:
#           - name: id
#             description: "The primary key for this table"
#             tests:
#                 - unique
#                 - not_null
